/**
 * TOMBSTONE (2026-07-21): FacilityRow was replaced by GeoRow.tsx in the
 * "market + map" v4 architecture — the per-facility list became a
 * one-row-per-country geographic footprint. This file only exists
 * because the sandbox cannot delete files on this machine:
 *   git rm components/hub/entity/FacilityRow.tsx
 * when committing.
 */

export {};
