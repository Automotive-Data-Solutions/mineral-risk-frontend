/**
 * TOMBSTONE (2026-07-22): merged into SubscribeBox.tsx (one reusable
 * subscribe box: eyebrow + copy + form). This file only remains because
 * the sandbox cannot delete device files — `git rm` it when committing.
 */

export {};
